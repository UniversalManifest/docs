# Worklog — 2026-02-12

## Stub fixtures (near-real)

Added richer “near-real” Universal Manifest v0.1 fixtures under:

- `examples/v0.1/stubs/`

These are intentionally **illustrative** (extra fields beyond the v0.1 context are allowed) and are meant to help:

- drive integration discussions (LAN edge/display/admin surfaces)
- provide realistic test data for validators and future tooling

## Stub fixtures index document

Added a human-first index of all v0.1 fixtures (baseline + stubs), with links to the driver docs per manifest section:

- `docs/STUB-MANIFESTS.md`

Added a draft adoption note for profile/social surfaces:

- `integrations/social.md`

## Project mapping + agent entry points

Added high-level project mapping and current status docs:

- `docs/DEPTH-AND-SCOPE.md`
- `docs/STATE-OF-THE-PROJECT.md`

Added a repo-level agent entrypoint:

- `AGENTS.md`

Added a draft “well-known names” registry for early interoperability:

- `spec/v0.1/REGISTRY.md`

## Conformance scaffolding (v0.1)

Added a v0.1 conformance checklist and initial invalid fixtures:

- `spec/v0.1/CONFORMANCE.md`
- `examples/v0.1/invalid/`

Added a baseline example demonstrating `@type` as an array:

- `examples/v0.1/type-array-manifest.jsonld`

Updated the fixture validator to treat `examples/v0.1/invalid/` as expected-fail fixtures.

## Signature profile draft (v0.2 direction)

Added a draft signature direction for the next version:

- `spec/v0.2/README.md`
- `spec/v0.2/SIGNATURE-PROFILE.md`

## Publishing + versioning plan

Added a plan for stable URLs, headers, caching, and immutability policy:

- `docs/PUBLISHING-AND-VERSIONING.md`

Added a draft release workflow:

- `docs/RELEASING.md`

Added lightweight work order tracking:

- `docs/workorders/WO-INDEX.md`

## Minimal validation “test”

Added a lightweight examples validation script to the TypeScript helper package:

- `packages/universal-manifest/scripts/validate-examples.mjs`

Run from `packages/universal-manifest/`:

- `npm test`

This builds the package and validates all `examples/v0.1/**/*.jsonld` fixtures using `assertUniversalManifestV01`.
