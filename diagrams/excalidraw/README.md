# Excalidraw Sources

Store `.excalidraw` files here as the editable source of truth for visual docs assets.

Current starter template reference:

- `site/public/diagrams/universal-manifest-overview-template.svg`

When the first authored Excalidraw source is created/exported, commit it here as:

- `universal-manifest-overview.excalidraw`
